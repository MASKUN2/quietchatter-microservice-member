server:
  port: 8081

spring:
  docker:
    compose:
      enabled: true
      file: compose.yaml
  
  cloud:
    consul:
      enabled: false # Disable consul for local development if not running
    aws:
      secretsmanager:
        enabled: false
    stream:
      kafka:
        binder:
          brokers: localhost:19092
